
const models = require(apiRoot + '/models');

module.exports = () => {
    // models.then(({
    //     User
    // }) => {
    //     User.create({
    //         firstName: 'John',
    //         lastName: 'Doe'
    //     }).then(() => {
    //         User.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Country
    // }) => {
    //     Country.create({
    //         name: 'Ukraine'
    //     }).then(() => {
    //         Country.findAll().then(console.log);
    //     })
    // });
    // models.then(({
    //     Roles
    // }) => {
    //     Roles.create({
    //         name: 'admin'
    //     }).then(() => {
    //         Roles.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     RoomsTypes
    // }) => {
    //     RoomsTypes.create({
    //         name: 'Twin room'
    //     }).then(() => {
    //         RoomsTypes.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Rooms
    // }) => {
    //     Rooms.create({
    //         price: 500,
    //         amount: 6,
    //         area: 89,
    //         description: 'Nice view from the window'
    //     }).then(() => {
    //         Rooms.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Properties
    // }) => {
    //     Properties.create({
    //         name: 'Lviv',
    //         address: 'Zelena 8b',
    //         description: 'description',
    //         taxes: 1.3,
    //         coordinates: { "lat": 41.650299987709538, "lng": 12.536399034779624 },
    //         rating: 7.9,
    //         contactPersonName: 'Jhon Doe',
    //         contactPhone: '9876543'
    //     }).then(() => {
    //         Properties.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Reviews
    // }) => {
    //     Reviews.create({
    //         content: 'Very nice place',
    //         createdAt: new Date(1918556565656)
    //     }).then(() => {
    //         Reviews.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Discount
    // }) => {
    //     Discount.create({
    //         discountRate: 1.7,
    //     }).then(() => {
    //         Discount.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Beds
    // }) => {
    //     Beds.create({
    //         name: 'twin bed'
    //     }).then(() => {
    //         Beds.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     Facility
    // }) => {
    //     Facility.create({
    //         name: 'Swimming Pool'
    //     }).then(() => {
    //         Facility.findAll().then(console.log);
    //     });
    // });

    // models.then(({
    //     FacilityCategory
    // }) => {
    //     FacilityCategory.create({
    //         name: 'SPA'
    //     }).then(() => {
    //         FacilityCategory.findAll().then(console.log);
    //     });
    // });


    // models.then(({
    //     Messages
    // }) => {
    //     Messages.create({
    //         body: 'Сan i arrive earlier?'
    //     }).then(() => {
    //         Messages.findAll().then(console.log);
    //     });
    // });
};

