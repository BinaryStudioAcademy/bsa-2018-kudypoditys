import defaultState from "client/logic/defaultState";

export default function propertyRegistration(state = defaultState.propertyRegistrationOptions, action) {
    switch (action.type) {
        default: {
            return state;
        }
    }
}